# Flag list sourced from cmd/clnkr/main.go — update when flags change.
_clnkr()
{
    local cur prev opts
    COMPREPLY=()
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"
    opts="-p --prompt --model --base-url --max-steps -v --verbose --version --help"

    # Flags that take a value — suppress completion for their arguments
    case "${prev}" in
        -p|--prompt|--model|--base-url|--max-steps)
            return 0
            ;;
    esac

    if [[ ${cur} == -* ]]; then
        COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
    fi
}
complete -F _clnkr clnkr
complete -F _clnkr clnku
complete -F _clnkr clnk
